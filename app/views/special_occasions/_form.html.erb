<%= form_for(@special_occasion) do |f| %>
  <% if @special_occasion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@special_occasion.errors.count, "error") %> prohibited this special_occasion from being saved:</h2>

      <ul>
      <% @special_occasion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="panel panel-primary">
      <div class="row">
        <div class="col-md-12"><br>
          <div class="panel-body">
              <div class="field">
                <p class="font">Occasion Name:</p>
                <%= f.text_field :name %>
              </div>


            <div class="panel-footer">
            <%= link_to 'Back', special_occasions_path, class:'btn btn-default' %><br><br>
            <%= link_to 'Home Page', root_path, class:'btn btn-default' %><br><br>

            <%= f.submit  class:"btn btn-default"%>
              </div>
              </div>
            </div>
          </div>
        </div>
            <% end %>